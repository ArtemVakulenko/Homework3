import React from 'react';
import './Header.scss';
// import Button from "../Button"

const Header = () => {
    return (
        <div className="header">
                hello from react
        </div>
    );
};

export default Header;
