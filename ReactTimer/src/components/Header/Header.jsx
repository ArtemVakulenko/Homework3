import React from 'react';
import Button from '../Button';
import './Header.scss';

const Header = () => {
    return (
        <div className="header">
            <Button value='change theme'/>
            this is header
        </div>
    );
};

export default Header;
