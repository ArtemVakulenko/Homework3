import React from 'react';
import './TimeBoard.scss';

const TimeBoard = () => {
    return (
        <div className="TimeBoard">
            this is time board
        </div>
    );
};

export default TimeBoard;
