import React from 'react';
import Timer from '../Timer';
import Header from '../Header/Header';

const App = () => {
        return (
            <>
                <Header/>
                <Timer/>
            </>
        );
}; 

export default App;
